spring:
  config:
    activate:
      on-profile: test
  mail:
    host: smtp.gmail.com
    port: 587
    username: test
    password: password

  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: never
